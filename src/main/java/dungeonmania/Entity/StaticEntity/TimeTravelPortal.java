package dungeonmania.Entity.StaticEntity;

import dungeonmania.util.Position;

public class TimeTravelPortal extends StaticEntity {

    public TimeTravelPortal(boolean isInteractable, Position position, String id, String type) {
        super(isInteractable, position, id, type);
    }
    
}
